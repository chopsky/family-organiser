/**
 * The app's store identities, backend-side. The web app carries its own
 * copies (web/src/lib/appReview.js / app-store.js) because the two
 * codebases don't share imports - if either ever changes, change both.
 */
module.exports = {
  APP_STORE_ID: '6762131562',
  PLAY_PACKAGE_ID: 'com.housemait.app',
};
